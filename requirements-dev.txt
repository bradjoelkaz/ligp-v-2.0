# =============================================================================
# IIGP v2.0 — lightweight dev/CI/runtime dependencies
# -----------------------------------------------------------------------------
# The full requirements.txt pulls heavy ML stacks (torch, prophet, bertopic, ...)
# that are NOT needed to run the API/admin/observability layer or to lint+test
# the Phase 8 code. CI uses this slim set so jobs stay fast and deterministic;
# production images add layer-specific extras on top.
# =============================================================================

# ---- Core / config ----------------------------------------------------------
pydantic>=2.7,<3.0
pydantic-settings>=2.2
PyYAML>=6.0
python-dotenv>=1.0
orjson>=3.10

# ---- API / web --------------------------------------------------------------
fastapi>=0.111
uvicorn[standard]>=0.30
jinja2>=3.1            # admin dashboard templates (Phase 8)
httpx>=0.27           # TestClient + integration requests

# ---- Observability ----------------------------------------------------------
prometheus-client>=0.20

# ---- Async task queue (Phase 17, CI runs tasks in eager mode) ---------------
celery[redis]>=5.4

# ---- Testing / quality ------------------------------------------------------
pytest>=8.2
pytest-asyncio>=0.23
pytest-cov>=5.0
ruff>=0.4
black>=24.4
mypy>=1.10


# ---- PostgreSQL compatibility (Phase 15, CI integration) --------------------
# psycopg3 to match the DBAdapter (`import psycopg`) and production requirements.
# The `pool` extra provides psycopg_pool.ConnectionPool (Phase 18 pooling).
psycopg[binary,pool]>=3.1
SQLAlchemy>=2.0       # alembic migrations during the postgres compat test
alembic>=1.13

# ---- Load testing (Phase 18, manual/headless; not collected by pytest) ------
locust>=2.29
