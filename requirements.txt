# =============================================================================
# IIGP v2.0 — Python dependencies
# Target runtime: Python 3.11
# Pinned with lower bounds; lock exact versions with `pip freeze` per environment.
# =============================================================================

# ---- Core / config ----------------------------------------------------------
pydantic>=2.7,<3.0
pydantic-settings>=2.2
PyYAML>=6.0
python-dotenv>=1.0
orjson>=3.10

# ---- Async ingestion / HTTP (Layer 1: DD-003) -------------------------------
aiohttp>=3.9
httpx>=0.27
tenacity>=8.2            # retry/backoff
pybreaker>=1.2          # circuit breaker
redis>=5.0              # TokenBucket distributed counter (QA-002)
feedparser>=6.0         # RSS collector

# ---- Normalization / dedup (Layer 2: QA-006/007) ----------------------------
datasketch>=1.6         # MinHash LSH
simhash>=2.1
fasttext-wheel>=0.9.2   # language detection (QA-007)
langdetect>=1.0.9

# ---- NLP / embeddings (Layers 3-5) ------------------------------------------
sentence-transformers>=2.7   # multilingual-mpnet-base-v2 (768dim, QA-013)
transformers>=4.41
torch>=2.2
bertopic>=0.16          # topic clustering
konlpy>=0.6             # Korean morphological analysis
spacy>=3.7

# ---- Time / seasonality (Layer 4: QA-012) -----------------------------------
prophet>=1.1
holidays>=0.50          # holidays-kr support
numpy>=1.26
pandas>=2.2
scipy>=1.13

# ---- Scoring / decision (Layers 6-8) ----------------------------------------
scikit-learn>=1.4
networkx>=3.3           # GraphStore Phase 1 backend (DD-006)
cvxpy>=1.4              # Markowitz portfolio constraint (DD-009)

# ---- Graph backends (Phase 2+, optional) ------------------------------------
neo4j>=5.20

# ---- Content generation / quality (Layer 9 / 9.5) ---------------------------
openai>=1.30
anthropic>=0.28
tiktoken>=0.7          # LLM token/cost tracking (QA-022)
bert-score>=0.3.13     # fact-check gate (DD-010)
textstat>=0.7          # Flesch-Kincaid readability

# ---- API / web --------------------------------------------------------------
fastapi>=0.111
uvicorn[standard]>=0.30
gunicorn>=22.0          # prod pre-fork master + UvicornWorker (Phase 16)
jinja2>=3.1             # admin dashboard templates (Layer 0 / Phase 8)

# ---- Orchestration / observability (Layer 0 / QA-028) -----------------------
prefect>=2.19
prometheus-client>=0.20

# ---- Storage / backup -------------------------------------------------------
SQLAlchemy>=2.0
psycopg[binary]>=3.1
qdrant-client>=1.9
boto3>=1.34             # S3/GCS snapshot (QA-029)

# ---- Testing / quality (QA-030) ---------------------------------------------
pytest>=8.2
pytest-asyncio>=0.23
pytest-cov>=5.0
hypothesis>=6.100       # property-based tests
ruff>=0.4
black>=24.4
mypy>=1.10
